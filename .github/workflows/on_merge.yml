name: Deploy Infra (prod)

on:
  push:
    branches: ["main"]
    paths:
      - "infra/**"
      - "lambda/**"

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infra

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Build Lambda zip (processor)
        run: |
          cd ../lambda/processor
          rm -f processor.zip
          zip -r processor.zip lambda_function.py
          unzip -l processor.zip | head

      - name: Terraform Init (remote state prod)
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=multilingual-audio-pipeline/prod/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="encrypt=true"

      - name: Terraform Apply (prod)
        run: terraform apply -auto-approve -var-file=prod.tfvars
